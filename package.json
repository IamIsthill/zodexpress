{
  "name": "zodware",
  "version": "1.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "push": "git push && git push --tags",
    "patch": "npm version patch && npm run push && npm publish",
    "minor": "npm version minor && npm run push && npm publish",
    "major": "npm version major && npm run push && npm publish"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "author": "IamIsthill",
  "license": "MIT",
  "description": "Lightweight, zero-dependency Express middleware for validating requests with Zod schemas — supports body, params, and query with automatic 422 responses.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/IamIsthill/zodexpress.git"
  },
  "bugs": {
    "url": "https://github.com/IamIsthill/zodexpress/issues"
  },
  "homepage": "https://github.com/IamIsthill/zodexpress#readme",
  "peerDependencies": {
    "express": ">=4.0.0"
  },
  "dependencies": {
    "zod": "^4.0.15"
  },
  "devDependencies": {
    "@jest-mock/express": "^3.0.0",
    "@jest/globals": "^30.0.5",
    "@types/express": "^5.0.3",
    "@types/jest": "^30.0.0",
    "jest": "^30.0.5",
    "ts-jest": "^29.4.1"
  }
}
